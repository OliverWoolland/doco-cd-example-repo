services:
  viewer:
    image: busybox:latest
    command: ["busybox", "httpd", "-f", "-p", "8080", "-h", "/www"]
    ports:
      - "8080:80"
    volumes:
      - ./test.md:/www/test.md:ro
    restart: unless-stopped
