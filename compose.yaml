services:
  viewer:
    image: busybox:latest
    container_name: example-viewer
    command: ["busybox", "httpd", "-f", "-p", "8080", "-h", "/www"]
    ports:
      - "8080:8080"
    volumes:
      - ./index.html:/www/index.html:ro
    restart: unless-stopped
